function operacoes(a,b){
    const adicao = a + b;   
    const subtracao = a - b;
    const divisao = a / b;
    const multiplicacão = a * b;
}
function executarOperacão(a, b, operacoes){
    return operacoes;
}

console.log(executarOperacão(6, 3, subtracao));